#ifndef CONVERSOES_H
#define CONVERSOES_H

char *decimal_para_bin_str(int n);

long binario_para_decimal(const char *bin);

long hex_para_decimal(const char *hex);

char *decimal_para_hex_str(int n);

#endif
